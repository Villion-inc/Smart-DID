services:
  backend:
    ports:
      - "3001:3000"

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        - VITE_BASE_PATH=
        - VITE_API_URL=/api
    ports:
      - "8080:80"
