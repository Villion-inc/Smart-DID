# Smart DID — 개발 기록

**기준:** `아산도서관_개발_Final.md` 84~300라인, `진행_체크리스트.md`  
**규칙:** 작업 1개 완료 시마다 "완료 일시", "수정/추가된 파일", "내용 요약" 기록

---

## 요약: 지금까지 한 것 / 앞으로 할 것

### 지금까지 한 것

| 구분 | 내용 |
|------|------|
| **파일 통합** | trailer-engine의 pipeline, qc, retry, services(gemini-client, gemini-client-v2, veo31, nanova-banana, subtitle-generator, video-processor)를 worker로 복사·병합. 최종적으로 **trailer-engine 기준 그대로 사용**하도록 worker 쪽 파일 교체 완료. |
| **공유 타입** | packages/shared에 pipeline.types.ts 병합. worker는 @smart-did/shared + worker/src/shared/types 재export 사용. |
| **Config** | worker config에 gemini, banana, veo, outputDir, tempDir, video, subtitle, backendUrl, internalApiSecret 등 trailer-engine·lib-mvp 통합. .env.example 보강. |
| **Backend 콜백** | `POST /api/internal/video-callback` 추가. Worker 완료/실패 시 bookId, status, videoUrl, subtitleUrl, errorMessage로 DB 갱신. |
| **Worker 콜백** | worker.ts에서 Pipeline V2 실행 후 성공/실패 시 notifyBackendVideoCallback 호출. (작업 13에서 진입점을 V2로 전환 완료) |
| **저장소 추상화** | StorageProvider 인터페이스, LocalStorageProvider, S3StorageProvider(스텁), getStorageProvider() factory. |
| **안전 검증** | prompt.service.validateSafety를 QC safetyKeywords 기반 isTextSafeForChildren 사용으로 통일. |
| **GeminiProvider** | GeminiProviderAdapter 구현. 키프레임=NanovaBananaClient(Banana.dev) 또는 플레이스홀더, 비디오=Veo31Client(imageBuffer base64 연동). |
| **테스트 기동 오류 수정** | Prisma 5 전용 스크립트, Worker용 shared 빌드 선행, Backend Fastify·@fastify/jwt 버전 호환, 테스트 준비 문서·Frontend type:module 추가. `npm run dev`로 Backend·Frontend·Worker 정상 기동 확인. |
| **Worker 진입점 Pipeline V2 전환** | worker.ts에서 videoGeneratorService 제거, PipelineOrchestratorV2 + GeminiProviderAdapter 호출로 교체. Job → VideoGenerationRequest 매핑, 결과(completed/failed)를 기존 Backend 콜백(READY/FAILED)으로 전달. |
| **프론트엔드 DID 영상 재생·프로토타입** | DidVideoPlayer 영상 URL 수정(/api 포함), /pip 라우트 추가, 프로토타입 fallback 샘플 영상, vite-env.d.ts 타입 보강. |
| **백엔드 기동 오류 수정** | dev 스크립트 tsx watch → tsx 변경(EPERM 방지), @fastify/cors·helmet·swagger·swagger-ui를 Fastify 5 호환 버전으로 업그레이드. |
| **Worker .env 로드 경로 수정** | config에서 .env 경로를 library-did-main/.env를 찾도록 다중 경로 시도, 기동 시 GEMINI/OPENAI/VEO 키 로드 여부 로그 추가. |
| **MVP Mock 도서 전환** | alpas.service → Mock 사용. DID·영상 요청이 BK001~BK035(35권) 기준으로 동작. /pip 기본 BK001. |
| **Sora API·콜백 401** | Sora 엔드포인트를 공식 스펙(POST/GET /v1/videos)으로 수정. 콜백 401 시 INTERNAL_API_SECRET 일치 안내. |
| **3장면 FFmpeg 병합** | trailer-engine의 VideoAssembler 사용. Pipeline V2 Step 7에서 성공한 장면을 temp 파일로 쓴 뒤 concat+자막 burn → storage 저장. FFmpeg 없으면 첫 장면만 저장 fallback. |
| **Admin 480px·DID 레이아웃** | Admin을 DID와 동일 480px 뷰포트로 통일. AdminLayout, 대시보드(API 현황·사서 추천·인기 책·이용자 로그), 추천도서 등록(/admin/recommend), 영상 관리 카드 리스트. "이 책은 여기 있어요!"는 위치 안내 페이지만 노출. /did 내 페이지 이동 시 스크롤바·가로 스크롤로 인한 흔들림 방지(scrollbar-gutter: stable, overflow-x hidden). |

### 앞으로 할 것

| 우선순위 | 항목 | 설명 |
|----------|------|------|
| **1** | FFmpeg 검증·폰트 | Pipeline V2에서 3장면 병합·자막 burn 연동 완료. FFmpeg 설치 시 동작. 자막 폰트(Noto Sans KR) 미설치 시 addSubtitles 실패 가능. |
| **2** | 프로덕션 DB | Prisma 스키마 이미 있음. PostgreSQL 전환 시 DATABASE_URL 변경, 마이그레이션 실행. subtitleUrl 마이그레이션 적용 확인. |
| **3** | S3 스토리지 실제 구현 | S3StorageProvider 스텁을 AWS SDK 연동으로 교체. presigned URL 생성. |
| **4** | ALPAS 실제 API | AD201(검색), AD206(상세), AE117(신착) 스켈레톤 완성. |
| **5** | 타입 통합 정리 | shared 타입과 worker/shared 재export 정리. 필요 시 네임스페이스 분리. |
| **선택** | Config DI | 싱글톤 → DI 패턴으로 테스트 용이화. |
| **선택** | WebSocket 진행률, BullMQ Dashboard, E2E 테스트 등 | 4.2·4.3절 항목. |

---

## 테스트용 환경변수

지금까지 작업한 Backend + Worker + 콜백·안전검증·GeminiProvider 등을 로컬에서 돌리기 위해 필요한 환경변수입니다.  
**위치:** `library-did-main/.env` (루트에 하나 두면 backend/worker가 공통으로 참조)

### 필수 (동작에 반드시 필요)

| 변수명 | 용도 | 예시 |
|--------|------|------|
| `DATABASE_URL` | Backend Prisma DB | `file:./packages/backend/prisma/dev.db` (SQLite) |
| `JWT_SECRET` | Backend 로그인·인증 | 32자 이상 문자열 |
| `INTERNAL_API_SECRET` | Worker → Backend 콜백 인증 (없으면 JWT_SECRET 사용) | **Backend와 Worker가 동일한 값이어야 함.** 불일치 시 콜백 401 Unauthorized |
| `REDIS_HOST` | BullMQ 큐 | `localhost` |
| `REDIS_PORT` | Redis 포트 | `6379` |
| `GEMINI_API_KEY` | Worker(GeminiClient 등) — **없으면 Worker 기동 시 에러** | Google AI Studio에서 발급 |
| `BACKEND_URL` | Worker가 콜백 호출할 Backend 주소 | `http://localhost:3000` |

### 선택 (기능별)

| 변수명 | 용도 | 비고 |
|--------|------|------|
| `BANANA_API_KEY`, `BANANA_MODEL_KEY` | Pipeline V2 키프레임 실제 이미지 (Banana.dev) | 없으면 키프레임은 플레이스홀더 PNG |
| `VEO_API_KEY` | Pipeline V2 실제 비디오 생성 (Veo 3.1) | 없으면 V2 비디오 단계에서 에러 (현재 진입점은 기존 플로우) |
| `VEO_PROJECT_ID`, `VEO_LOCATION` | Veo Vertex AI 프로젝트/리전 | 기본값: your-project-id, us-central1 |
| `PORT` | Backend 포트 | 기본 3000 |
| `STORAGE_TYPE`, `STORAGE_PATH` | 영상 저장 위치 | 기본 local, `./storage/videos` |
| `WORKER_CONCURRENCY` | 동시 Job 수 | 기본 2 |

### 최소 .env 예시 (복사해서 사용)

```env
# 필수
DATABASE_URL=file:./packages/backend/prisma/dev.db
JWT_SECRET=your-secret-key-change-in-production-32chars
INTERNAL_API_SECRET=your-secret-key-change-in-production-32chars
REDIS_HOST=localhost
REDIS_PORT=6379
GEMINI_API_KEY=여기에_실제_키
BACKEND_URL=http://localhost:3000

# 선택 (기본값으로 동작)
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
STORAGE_TYPE=local
STORAGE_PATH=./storage/videos
WORKER_CONCURRENCY=2
```

### 현재 테스트 가능 범위 (npm run dev 기준)

지금 로컬에서 `npm run dev`로 돌렸을 때 **어디까지 실제로 동작하는지** 정리한 내용입니다.

| 구분 | 구현 여부 | 비고 |
|------|------------|------|
| **Frontend** | ✅ 동작 | 로그인, 도서 검색, 상세, 영상 생성 요청, 상태 표시. Backend API 연동. |
| **Backend API** | ✅ 동작 | 인증, 도서 검색/상세, 영상 요청(큐 등록), DB(Prisma/SQLite), Swagger 문서. |
| **영상 요청 → 큐 등록** | ✅ 동작 | 요청 시 상태 `QUEUED`로 저장, BullMQ에 Job 추가. |
| **Worker Job 처리** | ✅ 동작 | Job 수신 → Pipeline V2(Book Grounding, Style Bible, 장면 계획·스크립트, 키프레임, Sora/Veo 영상, 자막, **3장면 FFmpeg 병합** → storage 저장). |
| **Worker → Backend 콜백** | ✅ 동작 | 완료 시 `READY` + videoUrl/subtitleUrl, 실패 시 `FAILED` + errorMessage 로 DB 갱신. |
| **상태 흐름** | ⚠️ 부분 | `QUEUED` → (Worker 완료 시) `READY` 또는 `FAILED`. **GENERATING**으로 바꾸는 “작업 시작” 콜백은 미구현. |
| **실제 영상 생성** | ✅ Sora/Veo | OPENAI_API_KEY(Sora) 또는 VEO_API_KEY 설정 시 실제 영상 생성. 키프레임은 BANANA 또는 플레이스홀더. |
| **Pipeline V2** | ✅ 연동 | Worker 진입점은 PipelineOrchestratorV2. 3장면 생성 후 VideoAssembler.concatAndSubtitles로 병합·자막 burn 후 저장. |
| **저장** | ✅ 로컬 | `STORAGE_TYPE=local` 기준 병합된 MP4를 `STORAGE_PATH`에 저장. FFmpeg 없으면 첫 장면만 저장(fallback). S3는 스텁. |

**정리:**  
- **E2E:** 영상 생성 요청 → 큐 → Worker Pipeline V2 → Sora/Veo 영상 생성 → FFmpeg 3장면 병합·자막 → storage 저장 → 콜백 → 프론트 상태·재생.  
- **필수:** Redis, FFmpeg(병합용), GEMINI_API_KEY, OPENAI_API_KEY 또는 VEO_API_KEY.

---

### 테스트 전 준비

1. **Redis 실행**  
   `redis-server` 또는 Docker: `docker run -d -p 6379:6379 redis`

2. **DB 마이그레이션**  
   `library-did-main` 루트에서:
   ```bash
   cd packages/backend && npx prisma migrate deploy
   # 또는 스키마만 반영: npx prisma db push
   npx prisma generate
   ```

3. **의존성**  
   worker에서: `npm install @google/generative-ai axios fluent-ffmpeg`  
   (이미 package.json에 있으면 생략)

4. **실행 순서**  
   Backend → Worker 순으로 기동. Frontend는 `REACT_APP_API_URL=http://localhost:3000/api` 로 Backend 지정.

---

## 완료된 작업 (상세)

### 작업 1 — pipeline 폴더 복사 (체크리스트 2번 #1)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | 없음 |
| **추가된 파일** | `library-did-main/packages/worker/src/pipeline/` 하위 전체 |
| **추가 경로** | `pipeline/assemble.ts`, `pipeline/orchestrator.ts`, `pipeline/planning/index.ts`, `pipeline/planning/scenePlanner.ts`, `pipeline/style/styleBible.ts`, `pipeline/grounding/index.ts`, `pipeline/grounding/buildBookFacts.ts`, `pipeline/grounding/fetchCandidates.ts`, `pipeline/grounding/rankCandidates.ts` |
| **내용 요약** | trailer-engine `src/pipeline/` 폴더를 lib-mvp Worker로 통째로 복사함. Book Grounding, Style Bible, Scene Planning, V2 오케스트레이터 등 파이프라인 코드가 worker 내부에 위치함. |
| **비고** | 복사본은 기존 trailer-engine 상대 경로(`../shared/types`, `../config`, `../worker/`, `../qc/`, `../anchor/`, `../cost/`, `../subtitles/`, `../cache/`)를 그대로 사용하므로, 아직 빌드 불가. 다음 작업에서 qc·retry·services·타입·config 복사·병합 후 import 경로 정리 예정. |

---

### 작업 2 — gemini-* 서비스 복사 + import 정리 (체크리스트 2번 #2)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/config/index.ts` — `geminiApiKey`, `gemini.model/temperature/maxRetries` 추가 |
| **추가된 파일** | `packages/worker/src/services/gemini-client.ts`, `packages/worker/src/services/gemini-client-v2.ts`, `packages/worker/src/types/index.ts` |
| **내용 요약** | trailer-engine의 `gemini-client.ts`, `gemini-client-v2.ts`를 worker/services로 복사. Worker에서 `../types`가 참조 가능하도록 `worker/src/types/index.ts`에 Scene, ScenePromptPayload, GeminiVideoResponse 타입 추가. Config에 GEMINI 관련 필드 추가하여 Gemini 클라이언트 생성자 동작 가능하도록 함. |
| **비고** | gemini-*는 `../config`, `../types` 사용 중. 타입은 추후 shared 패키지 병합 시 이전 예정. worker에서 `npm install @google/generative-ai` 실행 필요. |

---

### 작업 3 — sora-client 스킵 + veo31 / video-processor / subtitle-generator 복사 (체크리스트 2번 #3~#6)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/types/index.ts` — `SubtitleEntry` 타입 추가 |
| **추가된 파일** | `packages/worker/src/services/veo31-client.ts`, `packages/worker/src/services/video-processor.ts`, `packages/worker/src/services/subtitle-generator.ts` |
| **내용 요약** | **#3** trailer-engine에 `sora-client.ts` 없음 → 스킵. **#4** `veo31-client.ts` 복사 (Veo 3.1 Image-to-Video 클라이언트). **#5** `video-processor.ts` 복사 (FFmpeg 병합·자막·더미 비디오). **#6** `subtitle-generator.ts` 복사 (WebVTT/SRT 자막 생성). video-processor·subtitle-generator용으로 types에 `SubtitleEntry` 추가. |
| **비고** | 기존 `veo.service.ts`는 유지(진입점 연동 시 교체 예정). worker에서 `npm install fluent-ffmpeg` 및 `npm install -D @types/fluent-ffmpeg` 실행 필요. |

---

### 작업 4 — qc 폴더 복사 (체크리스트 2번 #7)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | 없음 |
| **추가된 파일** | `packages/worker/src/qc/` 하위 전체 |
| **추가 경로** | `qc/qcRunner.ts`, `qc/safetyGate.ts`, `qc/checkTypography.ts`, `qc/checkConsistency.ts`, `qc/scoreVideo.ts`, `qc/safetyKeywords.json`, `qc/thresholds.json`, `qc/typographyRules.json` |
| **내용 요약** | trailer-engine `src/qc/` 폴더를 worker로 통째로 복사. Safety Gate → Typography → Consistency → Video Scoring 순서의 QC 파이프라인 코드. |
| **비고** | qc 모듈은 `../shared/types`(QCReport, SceneScript, Anchor, TypographyPlan 등) 참조. 타입 통합(shared 병합) 후 import 경로 정리 예정. **sora-client.ts**: trailer-engine·lib-mvp 둘 다 해당 파일 없음(문서만 언급). |

---

### 작업 5 — retry 폴더 복사 (체크리스트 2번 #8, #9)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | 없음 |
| **추가된 파일** | `packages/worker/src/retry/hierarchicalRetry.ts`, `retry/retryPolicy.ts`, `retry/sceneGenerator.ts` |
| **내용 요약** | trailer-engine `src/worker/`의 hierarchicalRetry, retryPolicy, sceneGenerator를 worker `src/retry/`로 복사. 계층적 재시도(Script→Keyframe→Video), 장면별 재시도 정책, 병렬/순차 장면 생성 로직. |
| **비고** | `../shared/types`, `../qc/thresholds.json` 참조. qc는 이미 복사됨으로 thresholds.json 경로는 유효. shared 타입은 통합 후 정리. |

---

### 작업 6 — Backend 내부 콜백 API 추가 (체크리스트 1·3.3 A 연동)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/backend/src/config/index.ts` — `internalApiSecret` 추가, `packages/backend/src/index.ts` — internal 라우트 등록, `packages/backend/prisma/schema.prisma` — `subtitleUrl` 컬럼 추가 |
| **추가된 파일** | `packages/backend/src/routes/internal.routes.ts`, `packages/backend/prisma/migrations/20260209000000_add_subtitle_url/migration.sql` |
| **내용 요약** | Worker가 영상 생성 완료/실패 시 Backend DB를 갱신할 수 있도록 `POST /api/internal/video-callback` 추가. Body: bookId, status(READY\|FAILED), videoUrl?, subtitleUrl?, errorMessage?. Header: X-Internal-Secret. VideoRecord에 subtitleUrl 컬럼 추가(마이그레이션). |
| **비고** | 원격 DB 마이그레이션은 연결 이슈로 미적용. 로컬에서 `npx prisma migrate deploy` 또는 `prisma db push` 실행 필요. |

---

### 작업 7 — Worker 진입점 콜백 연동 (체크리스트 1·3.3 A)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/config/index.ts` — backendUrl, internalApiSecret 추가. `packages/worker/src/worker.ts` — 완료/실패 시 notifyBackendVideoCallback 호출 |
| **추가된 파일** | `packages/worker/src/services/backend-callback.service.ts` |
| **내용 요약** | Worker Job 처리 후 성공 시 READY + videoUrl/subtitleUrl, 실패 시 FAILED + errorMessage로 Backend 콜백 호출. 진입점은 당분간 기존 videoGeneratorService.generateVideo() 유지(파이프라인 V2 전환 시 GeminiProvider 구현 후 교체 예정). |
| **비고** | 환경변수: BACKEND_URL, INTERNAL_API_SECRET(또는 JWT_SECRET). |

---

### 작업 8 — 저장소 추상화 (체크리스트 3.3 D)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | 없음 |
| **추가된 파일** | `packages/worker/src/services/storage.provider.ts`, `storage-local.provider.ts`, `storage-s3.provider.ts`, `storage-provider.factory.ts` |
| **내용 요약** | StorageProvider 인터페이스(save/load/exists/getUrl) 정의. LocalStorageProvider(로컬 디스크), S3StorageProvider(스텁) 구현. getStorageProvider()로 config.storage.type에 따라 local/s3 반환. 기존 storage.service.ts는 유지. |
| **비고** | S3 실제 연동은 추후 구현. STORAGE_TYPE=local 기본. |

---

### 작업 9 — .env.example 보강 + Config 통합 (체크리스트 3.3 B, 7장)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `library-did-main/.env.example`, `packages/worker/src/config/index.ts` — openaiApiKey 추가 |
| **추가된 파일** | 없음 |
| **내용 요약** | .env.example에 GEMINI_*, BACKEND_URL, INTERNAL_API_SECRET, OUTPUT_DIR, TEMP_DIR, SUBTITLE_LANGUAGE, LOG_LEVEL, DATABASE_URL, S3 관련 등 반영. Worker config에 OPENAI_API_KEY 추가. |
| **비고** | 체크리스트 7장 환경변수 표와 동일하게 정리. |

---

### 작업 10 — QC safetyGate 연동 (체크리스트 4절 Critical #4)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/services/prompt.service.ts` — validateSafety에서 isTextSafeForChildren 사용 |
| **추가된 파일** | `packages/worker/src/qc/safetyCheck.ts` |
| **내용 요약** | 콘텐츠 안전 검증을 shared의 isContentSafe 대신 QC safetyGate와 동일한 safetyKeywords.json 기반 검사로 통일. safetyCheck.isTextSafeForChildren(title/summary)로 금지 단어·테마 검사 후 VideoGeneratorService에서 사용. |
| **비고** | 파이프라인 V2 진입 시 스크립트 단계에서는 기존 SafetyGate.validate(scripts, constraints) 유지. |

---

### 작업 11 — GeminiProvider 구현 + trailer-engine 서비스 그대로 사용 (2.2절 기준)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/services/gemini-client.ts`, `gemini-client-v2.ts`, `veo31-client.ts`, `nanova-banana-client.ts`, `subtitle-generator.ts`, `video-processor.ts`, `config/index.ts`(banana 추가) |
| **추가된 파일** | `packages/worker/src/services/gemini-provider.adapter.ts` |
| **내용 요약** | **GeminiProvider:** PipelineOrchestratorV2용 GeminiProviderAdapter 구현. generateKeyframe = BANANA 설정 시 NanovaBananaClient(이미지 생성 후 Buffer 반환), 미설정 시 플레이스홀더 PNG. generateVideo = Veo31Client.generateVideoFromImageBuffer(keyframeBuffer, prompt, duration). **서비스 파일:** 아산도서관_개발_Final.md 2.2절 “그대로 사용” 기준으로, gemini-client, gemini-client-v2, veo31-client, nanova-banana-client, subtitle-generator, video-processor를 library-trailer-engine-main 내용으로 교체. veo31에는 Pipeline V2용 imageBytesBase64·generateVideoFromImageBuffer 유지. nanova-banana에는 fetchImageAsBuffer 추가. |
| **비고** | .env.example에 BANANA_API_KEY, BANANA_MODEL_KEY 추가. |

---

### 작업 12 — 테스트 기동 오류 수정 (로컬 npm run dev 동작 확보)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `library-did-main/package.json`, `packages/backend/package.json`, `packages/frontend/package.json`, `테스트_준비_및_명령어.md`(경로·Prisma 안내 수정) |
| **추가된 파일** | `테스트_준비_및_명령어.md` (테스트 준비·명령어·트러블슈팅 정리) |
| **내용 요약** | **(1) Prisma:** 전역/최신 Prisma 7 사용 시 스키마 문법 오류 발생 → Backend에서 `npm run prisma:migrate:deploy`, `npm run prisma:generate` 로 로컬 Prisma 5 사용하도록 스크립트 추가. **(2) Worker @smart-did/shared:** shared 패키지 미빌드 시 "Cannot find module '@smart-did/shared'" → 루트 `dev:worker` 스크립트를 `npm run build --workspace=@smart-did/shared && npm run dev --workspace=@smart-did/worker` 로 변경해 기동 전 shared 빌드. **(3) Backend Fastify:** Fastify 5 + @fastify/cors 8·@fastify/jwt 10 조합에서 플러그인 버전 불일치 → Backend를 Fastify ^4.28.0, @fastify/jwt ^8.0.0 으로 맞춤. **(4) 테스트 문서:** DB 마이그레이션 경로(`packages/backend` 기준), npx prisma 대신 npm run 사용 안내 반영. **(5) Frontend:** postcss.config.js 관련 MODULE_TYPELESS_PACKAGE_JSON 경고 제거를 위해 `packages/frontend/package.json`에 `"type": "module"` 추가. |
| **비고** | Redis 실행, .env 필수값(GEMINI_API_KEY 등), `cd packages/backend` 후 Prisma 마이그레이션·generate 필요. 상세는 `테스트_준비_및_명령어.md` 참고. |

---

### 작업 13 — Worker 진입점 Pipeline V2 전환 (1단계)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-09 |
| **수정된 파일** | `packages/worker/src/worker.ts` |
| **추가된 파일** | 없음 |
| **내용 요약** | **진입점 전환:** `videoGeneratorService.generateVideo()` 호출 제거. `PipelineOrchestratorV2` + `GeminiProviderAdapter` 단일 인스턴스를 생성해 Job 처리 시 `pipelineOrchestrator.execute(request)` 호출. **매핑:** `VideoJobData`(bookId, title, author, summary) → `VideoGenerationRequest`(title, author, language: 'ko') 변환 함수 `toVideoGenerationRequest()` 추가. **결과 처리:** 반환값 `status === 'completed'`이면 `notifyBackendVideoCallback(READY, videoUrl, subtitleUrl)`, 그 외면 `notifyBackendVideoCallback(FAILED, errorMessage)`. 기존 Backend 콜백 형식·에러 시 재throw 동작 유지. |
| **비고** | Pipeline V2 내부 Step 7(Assembly)은 아직 placeholder. 실제 영상 파일 경로·FFmpeg 연동은 2단계(FFmpeg 실제 연동)에서 진행. |

---

### 작업 14 — 프론트엔드: DID 영상 재생·프로토타입(/pip) 및 영상 URL 수정

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/frontend/src/pages/did/DidVideoPlayer.tsx`, `packages/frontend/src/App.tsx` |
| **추가된 파일** | `packages/frontend/src/vite-env.d.ts` |
| **내용 요약** | **(1) 영상 URL 수정:** 백엔드는 `GET /api/videos/:filename`으로 서빙하는데, 프론트가 상대 경로 videoUrl을 조합할 때 `/api`를 제거해 `.../videos/xxx.mp4`로 요청하던 문제 수정. API base를 `/api` 포함한 값으로 두어 `.../api/videos/xxx.mp4`로 요청하도록 변경. **(2) /pip 라우트:** 프로토타입용으로 `/pip` → `/did/video/ISBN-001` 리다이렉트, `/pip/:bookId` → DidVideoPlayer 표시 추가. **(3) 프로토타입 fallback:** API 영상 로드 실패 시 onError에서 샘플 영상 URL로 전환; 에러/생성 중 상태에서 videoUrl이 없으면 3초 후 동일 샘플 재생. `VITE_PROTOTYPE_FALLBACK_VIDEO`로 URL 오버라이드 가능. **(4) 타입:** import.meta.env 타입 에러 방지를 위해 vite-env.d.ts 추가, DidVideoPlayer에서 env를 변수로 추출해 사용. |
| **비고** | 시드 도서 ISBN-001에 READY 영상이 있으면 /pip에서 재생. 없거나 404면 fallback 샘플 재생. |

---

### 작업 15 — 백엔드 기동 오류 수정 (tsx watch·Fastify 5 플러그인)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/backend/package.json` |
| **추가된 파일** | 없음 |
| **내용 요약** | **(1) tsx watch 제거:** dev 스크립트를 `tsx watch src/index.ts` → `tsx src/index.ts`로 변경. 일부 환경에서 `tsx watch`가 IPC 파이프 생성 시 EPERM(operation not permitted) 발생하므로 watch 없이 실행하도록 수정. **(2) Fastify 5 플러그인 호환:** 백엔드는 Fastify 5.7.4 사용 중인데, 기존 @fastify/* 플러그인이 Fastify 4용이라 "expected '4.x' fastify version" 오류 발생. 다음 버전으로 업그레이드: @fastify/cors ^8.5.0 → ^10.0.0, @fastify/helmet ^11.1.1 → ^12.0.0, @fastify/swagger ^8.13.0 → ^9.0.0, @fastify/swagger-ui ^2.1.0 → ^5.0.0. |
| **비고** | 코드 수정 후 자동 재실행이 필요하면 nodemon 등으로 dev 스크립트 교체 가능. |

---

### 작업 16 — Worker .env 로드 경로 수정 및 기동 시 키 로드 로그

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/worker/src/config/index.ts`, `packages/worker/src/index.ts` |
| **추가된 파일** | 없음 |
| **내용 요약** | **(1) .env 경로 수정:** config의 dotenv가 `path.resolve(__dirname, '../../../.env')`를 사용해 `packages/.env`를 참조하고 있어, 실제 .env가 있는 `library-did-main/.env`를 읽지 못함. `library-did-main/.env`를 찾도록 여러 경로(../../../../.env, ../../../.env, process.cwd() 기준 .env 등)를 순서대로 시도하도록 변경. **(2) 기동 시 키 로드 로그:** Worker 기동 시 GEMINI_API_KEY, OPENAI_API_KEY(Sora), VEO_API_KEY가 set/NOT SET인지만 로그로 출력(키 값은 노출하지 않음). 키 미설정 시 403·No video provider 원인 확인 용이. |
| **비고** | .env는 `library-did-main/.env` 한 곳에 두고, Worker 재시작 후 로그에서 "set" 확인. |

---

### 작업 17 — MVP: Mock 도서로 영상 생성 (ALPAS Mock 전환)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/backend/src/services/alpas.service.ts`, `packages/frontend/src/App.tsx` |
| **추가된 파일** | 없음 |
| **내용 요약** | **(1) Backend:** `alpas.service.ts`를 실제 ALPAS API 대신 **Mock**(`alpas.service.mock.ts`)을 사용하도록 변경. DID·도서 상세·검색·영상 요청이 모두 **MOCK_BOOKS**(BK001~BK035, 35권) 기준으로 동작. 실제 API 사용 시 `alpas.service.ts`에서 `from './alpas-real.service'` 로 export만 바꾸면 됨. **(2) Frontend:** 프로토타입 `/pip` 기본 이동을 ISBN-001 → **BK001**(Mock 도서 ID)로 변경. |
| **비고** | Mock 도서 35권으로 DID 테마별 목록·영상 생성 요청·Worker 파이프라인 E2E 검증 가능. |

---

### 작업 18 — Sora API 엔드포인트 수정 및 콜백 401 안내

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/worker/src/services/sora-client.ts`, `packages/worker/src/services/veo.service.ts`, `.env.example`, `개발 docs/개발_기록.md` |
| **추가된 파일** | 없음 |
| **내용 요약** | **(1) Sora 405 수정:** Worker가 사용하던 `POST /v1/videos/generations`는 OpenAI 공식 스펙이 아님. 공식은 `POST /v1/videos`(생성) → `GET /v1/videos/{id}`(폴링) → `GET /v1/videos/{id}/content`(다운로드). sora-client.ts를 위 스펙에 맞게 수정(model: sora-2, seconds, size 등). **(2) VeoService 호환:** Sora가 이제 URL이 아닌 buffer만 반환하므로, veo.service의 generateWithSora에서 videoBuffer일 때 임시 파일로 저장 후 file URL 반환하도록 처리. **(3) 콜백 401:** Worker→Backend 콜백 시 401이 나오면 Backend와 Worker의 `INTERNAL_API_SECRET`(또는 JWT_SECRET fallback) 값이 **동일한지** 확인. .env 한 곳(library-did-main/.env)을 두 앱이 공통으로 쓰면 일치함. |
| **비고** | Sora API는 조직 검증·초대 기반 접근일 수 있음. 405 해결 후에도 403/402 등이 나오면 OpenAI 대시보드에서 비디오 API 권한 확인. |

---

### 작업 19 — 3장면 FFmpeg 병합 적용 (trailer-engine assemble 연동)

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-11 |
| **수정된 파일** | `packages/worker/src/pipeline/assemble.ts`, `packages/worker/src/pipeline/orchestrator.ts` |
| **추가된 파일** | 없음 |
| **내용 요약** | **(1) assemble.ts:** trailer-engine과 동일한 VideoAssembler에 **concatAndSubtitles(scenes, subtitlePath, outputPath)** 추가. concat(FFmpeg) → 자막 burn만 수행(타이틀 카드 제외). concat 리스트는 **파일명만** 사용해 Windows/경로 이슈 방지. addSubtitles에서 경로 **백슬래시 → 슬래시** 정규화. **(2) orchestrator Step 7:** 성공한 장면을 sceneNumber 순으로 정렬 후, tempDir에 scene-1.mp4·scene-2.mp4·scene-3.mp4와 VTT 파일 기록 → VideoAssembler.concatAndSubtitles 호출 → 병합된 파일을 storage에 저장 후 videoUrl 반환. FFmpeg 미설치 또는 병합 실패 시 **첫 번째 장면만** 저장하는 fallback 유지. **(3) 정리:** temp 디렉터리 삭제, 불필요한 import(TypographyPlan) 제거. |
| **비고** | FFmpeg 설치 필요. 자막 burn 시 Noto Sans KR 등 지정 폰트가 없으면 addSubtitles 단계에서 실패할 수 있음(실패 시 fallback으로 첫 장면만 저장). |

---

### 작업 20 — Admin 페이지 DID 동일 뷰포트(480px) 및 DID 레이아웃 흔들림 방지

| 항목 | 내용 |
|------|------|
| **완료 일시** | 2026-02-13 |
| **수정된 파일** | `packages/frontend/src/App.tsx`, `packages/frontend/src/pages/admin/Login.tsx`, `packages/frontend/src/pages/admin/Dashboard.tsx`, `packages/frontend/src/pages/admin/VideoManagement.tsx`, `packages/frontend/src/pages/did/DidV2BookDetail.tsx`, `packages/frontend/src/pages/did/DidV2Layout.tsx`, `packages/frontend/src/index.css` |
| **추가된 파일** | `packages/frontend/src/pages/admin/AdminLayout.tsx`, `packages/frontend/src/pages/admin/AdminRecommendBook.tsx` |
| **내용 요약** | **(1) Admin 레이아웃:** DID와 동일한 480px 뷰포트로 `AdminLayout` 추가. 상단 검정 헤더 "BOOK MATE 관리자", 대시보드 외 페이지에서 "← 대시보드" 버튼 표시. **(2) Admin 대시보드:** API 사용 현황(총 생성 수, 사용 비용, 잔여 예산, 생성 가능 개수), 사서 추천 도서 추가(책 제목 + "+ 추천도서 등록" 버튼 → /admin/recommend), 인기 사용 책, 이용자 생성 로그(검색·테이블 헤더·빈 상태), 하단 빨간 안내 문구. **(3) 추천도서 등록:** `/admin/recommend` 페이지 추가. 제목, 연령대, 질문(자막), 태그, 커버(이모지), 존 생상, 책장/줄, "📒 등록하기 (메인에 노출)" 버튼. **(4) 영상 관리:** AdminLayout 적용, 480px에 맞춰 카드 리스트 형태로 표시. **(5) 로그인:** 480px 중앙 래퍼로 DID와 동일 너비. **(6) 책 미리보기:** "이 책은 여기 있어요! {제목}" 문구를 책 미리보기(DidV2BookDetail)에서 제거 → 위치 안내 페이지(DidV2Location)에서만 노출. **(7) /did 페이지 이동 시 레이아웃 흔들림 방지:** DidV2Layout의 main에 `scrollbar-gutter: stable` 적용, 레이아웃 루트에 `overflow-x-hidden`·`max-w-[480px]`. index.css에 html `scrollbar-gutter: stable`, body `overflow-x: hidden` 추가. |
| **비고** | 관리자 기본 계정: admin / admin1234. 추천도서 등록 제출은 백엔드 API 연동 후 연동 예정. |

---

## 앞으로 할 것 (상세)

- **FFmpeg:** video-processor 실제 연동 검증, fluent-ffmpeg 의존성 설치.
- **DB:** Prisma 마이그레이션 적용(subtitleUrl 등), 프로덕션 시 PostgreSQL 전환.
- **S3:** S3StorageProvider 실제 구현, presigned URL.
- **ALPAS API, WebSocket, E2E 등:** 진행_체크리스트 4.2·4.3절 참고.

---

*마지막 업데이트: 2026-02-13 (작업 20 Admin 480px·DID 레이아웃 흔들림 방지)*
