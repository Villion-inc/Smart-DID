# 아산도서관 Smart DID — 진행 체크리스트

**기준:** `아산도서관_개발_Final.md` 84~300라인  
**목표:** 내용 손실 없이 “내가 진행해야 할 것” 전부 나열

---

## 1. 통합 아키텍처 (3.1절)

- [ ] **Worker 영상 생성 흐름 교체**
  - 현재: `BullMQ Job → videoGeneratorService.generateVideo() → veoService.generateScene() [MOCK] → mergeScenes() [MOCK] → 가짜 URL`
  - 목표: `BullMQ Job → PipelineOrchestratorV2.run(bookTitle) → Gemini Script → Sora/Veo Video → QC Pipeline → FFmpeg 병합 → S3 업로드 → 실제 URL 반환`
- [x] **Worker 완료/실패 시 Backend 콜백**  
  - Job 데이터에서 `bookId` 등 추출 → 결과물(`videoUrl`, `subtitleUrl`)을 Backend `POST /api/internal/video-callback`으로 전달하여 DB 갱신(READY/FAILED) ✅

---

## 2. 파일 이동·복사 (3.2절)

| # | trailer-engine 소스 | lib-mvp 대상 | 작업 내용 |
|---|--------------------|-------------|-----------|
| 1 | `src/pipeline/` | `packages/worker/src/pipeline/` | 폴더 통째로 복사 ✅ |
| 2 | `src/services/gemini-*` | `packages/worker/src/services/` | 복사 + import 경로 수정 ✅ |
| 3 | `src/services/sora-client.ts` | `packages/worker/src/services/` | 복사 (해당 파일 없음, 스킵) ✅ |
| 4 | `src/services/veo31-client.ts` | `packages/worker/src/services/` | **기존 veo.service.ts 대체** ✅ |
| 5 | `src/services/video-processor.ts` | `packages/worker/src/services/` | **기존 mergeScenes() 로직 대체** ✅ |
| 6 | `src/services/subtitle-generator.ts` | `packages/worker/src/services/` | 복사 ✅ |
| 7 | `src/qc/*` | `packages/worker/src/qc/` | 폴더 통째로 복사 ✅ |
| 8 | `src/worker/hierarchicalRetry.ts` | `packages/worker/src/retry/` | 복사 ✅ |
| 9 | `src/worker/sceneGenerator.ts` | `packages/worker/src/retry/` | 복사 ✅ |
| 10 | `src/shared/types/index.ts` | `packages/shared/src/types/` | **기존 타입과 병합** (pipeline.types.ts) ✅ |
| 11 | `src/config/index.ts` | `packages/worker/src/config/` | **기존 config와 병합** ✅ |

---

## 3. 핵심 수정 사항 (3.3절)

### A. Worker 진입점

- [x] **파일:** `packages/worker/src/worker.ts` — 콜백 연동 완료
- [ ] `videoGeneratorService.generateVideo()` 제거 (Pipeline V2 전환 시)
- [ ] `PipelineOrchestratorV2.run()` 호출로 교체 (GeminiProvider 구현 후)
- [x] Job 데이터에서 bookId 추출 후 결과를 **Backend API에 콜백** (READY/FAILED, videoUrl/subtitleUrl/errorMessage) ✅

### B. Config 통합

- [x] **위치:** `packages/worker/src/config/` — 병합 완료
- [x] trailer-engine용 설정 추가: `GEMINI_API_KEY`, `OPENAI_API_KEY`, `outputDir`, `tempDir`, `video.*`, `subtitle` 등
- [x] lib-mvp Worker 기존 config와 **병합**
- [ ] 싱글톤 → **DI** (선택, 추후 테스트 용이화 시)

### C. 타입 통합

- [ ] **위치:** `packages/shared/src/types/`
- [ ] trailer-engine 쪽 핵심 타입 이전: `VideoGenerationRequest`, `SceneResult`, `QCReport` 등
- [ ] lib-mvp 기존 `VideoStatus` enum 등과 **충돌하지 않도록 네임스페이스 분리**

### D. 저장소 추상화

- [x] **StorageProvider 인터페이스** 정의 (`storage.provider.ts`) ✅
- [x] **LocalStorage** 구현 (`storage-local.provider.ts`) ✅
- [x] **S3Storage** 스텁 구현 (`storage-s3.provider.ts`, 추후 연동) ✅
- [x] `getStorageProvider()`로 Local / S3 **교체 가능** 구성 ✅

---

## 4. Critical — MVP 차단 (4.1절, 반드시 해결)

| # | 항목 | 파일/위치 | 현재 | 해야 할 일 | 예상 |
|---|------|-----------|------|------------|------|
| 1 | trailer-engine 통합 | worker/src/services/ | Mock | 3.2절 파일 이동 + 3.3절 수정 | 3~4일 |
| 2 | FFmpeg 영상 병합 | worker (video-processor 등) | Stub | **fluent-ffmpeg** 연동 | 1~2일 |
| 3 | 프로덕션 DB | backend/src/db/ | 인메모리 Map | **PostgreSQL + Prisma** 전환 | 2~3일 |
| 4 | 콘텐츠 안전 검증 | worker (videogenerator 등) | QC 키워드 연동 | **safetyCheck + safetyKeywords** 사용 ✅ | 완료 |

---

## 5. Important — 기능 완성 (4.2절)

- [ ] **S3 클라우드 스토리지**  
  - `storage.service.ts`에 S3 업로드 구현  
  - **presigned URL** 생성  
  - 예상: 1~2일
- [ ] **ALPAS 실제 API**  
  - `alpas-real.service.ts` 스켈레톤 완성  
  - AD201(검색), AD206(상세), AE117(신착) 3개 API  
  - 예상: 1일
- [ ] **Prisma 스키마 마이그레이션**  
  - `prisma/schema.prisma` 이미 정의됨  
  - 마이그레이션 실행 + 리포지토리 전환  
  - 예상: 1일
- [ ] **WebSocket 실시간 알림**  
  - 영상 생성 진행률을 DID 키오스크에 실시간 전달  
  - 예상: 1일

---

## 6. Nice-to-have — 프로덕션 개선 (4.3절)

- [ ] BullMQ Dashboard (모니터링 UI)
- [ ] Prometheus + Grafana 메트릭
- [ ] E2E 테스트 (Frontend ↔ Backend ↔ Worker)
- [ ] 비용 최적화 자동화 (모델 자동 선택)
- [ ] DID 키오스크 전용 터치 최적화 완성

---

## 7. 환경 변수 및 시크릿 (5장)

- [x] **.env.example에 아래 전부 반영** (GEMINI, BACKEND_URL, INTERNAL_API_SECRET, OUTPUT_DIR, LOG_LEVEL 등) ✅

| 변수명 | 용도 | 필수 |
|--------|------|------|
| GEMINI_API_KEY | Gemini 2.0 Flash | 필수 |
| OPENAI_API_KEY | Sora 2 영상 생성 | 필수 |
| COMET_API_KEY | Comet Sora (백업) | 선택 |
| REDIS_HOST | BullMQ 큐 (기본 localhost) | 필수 |
| REDIS_PORT | Redis 포트 (기본 6379) | 필수 |
| VEO_API_ENDPOINT | Veo 3.1 API (Vertex AI 등) | 선택 |
| AWS_ACCESS_KEY_ID | S3 | 프로덕션 필수 |
| AWS_SECRET_ACCESS_KEY | S3 | 프로덕션 필수 |
| S3_BUCKET_NAME | 영상 버킷 (리전 ap-northeast-2) | 프로덕션 필수 |
| DATABASE_URL | PostgreSQL (Prisma) | 프로덕션 필수 |
| JWT_SECRET | 관리자 인증 (32자 이상) | 필수 |
| PORT | 서버 포트 (기본 3000) | 선택 |
| NODE_ENV | development / production | 선택 |
| STORAGE_TYPE | local / s3 | 선택 |

---

## 8. 주간 스프린트 — Week 1 (6절 D1~D5)

| 일차 | 작업 | 담당/산출물 | 의존성 |
|------|------|-------------|--------|
| D1 | trailer-engine 파일 복사 + import 정리 | worker/src/pipeline, qc, retry/ | 없음 |
| D1 | Config 통합 (env 변수 병합) | worker/src/config/ | 없음 |
| D2 | 타입 통합 (shared 패키지) | shared/src/types/ | D1 |
| D2 | Worker 진입점 수정 (BullMQ → Pipeline + Backend 콜백) | worker/src/worker.ts | D1 |
| D3 | FFmpeg 영상 병합 실제 구현 | worker/src/services/video-processor | D2 |
| D3 | Storage 추상화 (Local + S3) | worker/src/services/storage | 없음 |
| D4 | PostgreSQL + Prisma 마이그레이션 | backend/src/db/ | 없음 |
| D4 | Repository 전환 (Map → Prisma) | backend/src/repositories/ | D4 DB |
| D5 | E2E 통합 테스트 (한 권 영상 생성) | 전체 파이프라인 | D1~D4 |

---

## 9. 주간 스프린트 — Week 2 (6절 D6~D10)

| 일차 | 작업 | 담당/산출물 | 의존성 |
|------|------|-------------|--------|
| D6 | ALPAS 실제 API 연동 완성 | backend/src/services/alpas | 없음 |
| D6 | QC safetyGate → Worker 안전 검증 교체 | worker/src/services/ | W1 |
| D7 | S3 업로드 + presigned URL 구현 | worker/src/services/storage | AWS 계정 |
| D7 | Docker Compose 통합 빌드 테스트 | docker-compose.yml | 없음 |
| D8 | Frontend ↔ Backend E2E 테스트 | 전체 시스템 | D6~D7 |
| D8 | DID 키오스크 UI 최종 검증 | frontend/src/pages/Did* | D8 E2E |
| D9 | 시크릿 관리 + 배포 스크립트 | ops/ | 없음 |
| D9 | 에러 핸들링 + 로깅 정리 | 전체 | 없음 |
| D10 | 부하 테스트 + 버그 수정 + 릴리스 | 전체 시스템 | D6~D9 |

---

## 10. API 참고 (8절 — 구현 확인용)

- Public DID: `GET/POST /api/did/...` (new-arrivals, search, books/:bookId, video, video/request, videos/popular)
- Admin: `POST /api/auth/login`, `GET /api/admin/dashboard/stats`, seed, queue/stats, queue/:bookId 취소·재시도, cache/stats, cache/cleanup  
→ 문서와 동일하게 동작하는지 확인.

---

## 11. 인수인계 체크리스트 (9절)

### 9.1 사전 준비

- [ ] Google AI Studio에서 GEMINI_API_KEY 발급 확인
- [ ] OpenAI API에서 OPENAI_API_KEY 발급 + Sora 2 접근 권한 확인
- [ ] AWS 계정 + S3 버킷(ap-northeast-2) 생성
- [ ] PostgreSQL 인스턴스 준비 (로컬 또는 AWS RDS)
- [ ] Redis 인스턴스 준비 (로컬 또는 ElastiCache)
- [ ] FFmpeg 바이너리 설치 (Docker 이미지에 포함 필요)
- [ ] Node.js 18+ 환경 확인

### 9.2 코드 체크아웃 후 확인

- [ ] npm install (루트 + 각 패키지)
- [ ] .env 파일 생성 (5장 환경 변수 참조)
- [ ] docker-compose up redis (Redis 먼저 실행)
- [ ] npm run dev (3개 서비스 동시 실행 확인)
- [ ] http://localhost:3000/health (Backend 헬스체크)
- [ ] http://localhost:5173 (Frontend 접속 확인)

### 9.3 통합 완료 후 검증

- [ ] 단일 도서 영상 생성 E2E: DID UI → 요청 → 큐 → Worker → 영상 → 재생
- [ ] QC 점수 85점 이상 확인
- [ ] DB에 영상 기록 영속 확인 (서버 재시작 후에도 유지)
- [ ] 캐시 정리 스케줄러 동작 확인
- [ ] 관리자 대시보드에서 큐/캐시 통계 표시 확인

---

## 12. 문서 간 불일치 (7절 참고)

- README.md: Express.js → 실제는 Fastify. 문서 업데이트 필요.
- 프로덕션 영상: 24초/3장면(8초) 기준. 20초/2장면 실험 설정은 프로덕션 아님.
- 제안서: Veo 3.1만 언급 → 실제는 Sora 2 기본. 제안서 업데이트 필요.

---

**이 체크리스트는 `아산도서관_개발_Final.md` 84~300라인 내용을 빠짐없이 작업 항목으로 옮긴 것입니다.**
